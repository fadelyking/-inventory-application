<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Categories</title>
	</head>
	<body>
<%-include('./nav') %>
		<ul>
			<% for (category of category_list) { %>
			<li><a href="<%=category.url %>"><%=category.name %></a><a href="<%= category.url + '/delete' %>">DELETE</a><a href="<%= category.url + '/update' %>">	UPDATE</a></li>
			<% } %>
		</ul>
	</body>
</html>
